server:
  port: 8888


spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/BahMartins/microservice-repo.git
          search-paths:
          - supplier
          - store
          - transporter